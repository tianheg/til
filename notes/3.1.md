---
layout: note
title: Keep `footer` at the Bottom
parent: CSS
grand_parent: Home
---

# Keep `footer` at the Bottom

```css
footer {
  position: absolute;
  bottom: 0;
}
```

Search Google with `css display footer only view page do not scroll`
