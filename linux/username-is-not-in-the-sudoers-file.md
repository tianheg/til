# Username Is Not In The Sudoers File

Login as `root`:

```bash
root@hostname: adduser username sudo
Adding user `username` to group `sudo` ...
Adding user username to group sudo
Done.
```

[source](https://unix.stackexchange.com/a/258865)
