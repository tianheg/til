# Use H1 Tag Write Helloworld

Just input:

```html
<h1>Helloworld</h1>
```

Most HTML elements have an opening tag and a closing tag. The only difference between opening and closing tags is the forward slash[^1] after the opening bracket of a closing tag.

[^1]: 正斜杠

[source](https://www.freecodecamp.org/learn/responsive-web-design/basic-html-and-html5/say-hello-to-html-elements)
